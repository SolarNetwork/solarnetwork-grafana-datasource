apiVersion: 1

datasources:
  - name: 'solarnetwork-datasource'
    type: 'solarnetwork-solarnetwork-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      token: $SN_API_TOKEN
    secureJsonData:
      secret: $SN_API_SECRET
